#!/bin/sh
# ORT's static provider loader derives its directory from the invoked executable
# path. Resolve the release first; invoking a bin/ree symlink directly is unsafe
# for provider discovery even though the ELF binary itself has $ORIGIN RUNPATH.
set -eu
launcher=$(readlink -f -- "$0")
exec "${launcher%/*}/ree" "$@"
